<%- include('partials/head') %>

<main>
    <div class="title">
        <h1><%= title %></h1>
        <p class="sub-title">enquête</p>
    </div>

    <form action="/questions" class="form form-name">
        <div class="form-group">
            <label for="firstname">Voornaam</label>
            <input class="form-control" type="text" id="firstname" name="firstname" placeholder="John" value="<%= queries.firstname %>" required="required"/>
        </div>
        <div class="form-group">
            <label for="lastname">Achternaam</label>
            <input class="form-control" type="text" id="lastname" name="lastname" placeholder="Doe" value="<%= queries.lastname %>" required="required"/>
        </div>
        <div class="links">
            <button type="submit" class="full-width-button">Start</button>
        </div>
    </form>
</main>

<%- include('partials/footer') %>