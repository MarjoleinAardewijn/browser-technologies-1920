<%- include('partials/head') %>

<main>
    <div class="title">
        <h1><%= title %></h1>
        <p class="sub-title">enquête</p>
    </div>

    <form action="/questions" class="form form-name">
        <div class="form-group">
            <label><span>Voornaam</span>
                <input class="form-control" type="text" name="firstname" placeholder="John" value="<%= queries.firstname %>" required="required"/>
            </label>
        </div>
        <div class="form-group">
            <label>
                <span>Achternaam</span>
                <input class="form-control" type="text" name="lastname" placeholder="Doe" value="<%= queries.lastname %>" required="required"/>
            </label>
        </div>
        <div class="links">
            <button type="submit" class="full-width-button">Start</button>
        </div>
    </form>
</main>

<%- include('partials/footer') %>